@page
@model EksamenProjekt2Sem.Pages.Order.ReadAllOrdersModel
@{
}

<h1>Alle ordrer</h1>

<form method="post">
    <input type="text" asp-for="SearchString" placeholder="Søg efter ingrediens..." />
    <button type="submit">Søg</button>
</form>

@if (Model.Orders != null && Model.Orders.Any())
{
    <ul>
        @foreach (var order in Model.Orders)
        {
            double totalPrice = 0;

            <li>
                <strong>Bruger:</strong> @order.User.Name<br />
                <strong>Dato:</strong> @order.PickupTime<br />
                <ul>
                    @foreach (var line in order.OrderLines)
                    {
                        <li>
                            <p>@line.Food.Ingredients, @line.Quantity stk., @line.Food.Price kr.</p>
                        </li>

                        totalPrice += line.Food.Price * line.Quantity;
                    }
                </ul>
                <strong>Samlet pris: @totalPrice.ToString("0.00") kr.</strong>
            </li>
        }
    </ul>
}
else
{
    <p>Ingen ordrer fundet.</p>
}